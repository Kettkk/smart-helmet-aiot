# Use local, non-production values only. Do not commit a populated .env file.
SPRING_DATASOURCE_URL=jdbc:mysql://localhost:3306/helmi?serverTimezone=UTC
SPRING_DATASOURCE_USERNAME=helmi_app
SPRING_DATASOURCE_PASSWORD=replace-with-a-local-password

MQTT_HOST=localhost
MQTT_PORT=1883
MQTT_USERNAME=local-user
MQTT_PASSWORD=replace-with-a-local-password
